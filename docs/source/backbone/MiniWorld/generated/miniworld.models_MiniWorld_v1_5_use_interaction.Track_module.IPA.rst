miniworld.models\_MiniWorld\_v1\_5\_use\_interaction.Track\_module.IPA
======================================================================

.. currentmodule:: miniworld.models_MiniWorld_v1_5_use_interaction.Track_module

.. autoclass:: IPA

   
   .. automethod:: __init__

   
   .. rubric:: Methods

   .. autosummary::
   
      ~IPA.__init__
      ~IPA.add_module
      ~IPA.apply
      ~IPA.apply_RT
      ~IPA.bfloat16
      ~IPA.buffers
      ~IPA.children
      ~IPA.compile
      ~IPA.cpu
      ~IPA.cuda
      ~IPA.double
      ~IPA.eval
      ~IPA.extra_repr
      ~IPA.float
      ~IPA.forward
      ~IPA.gather_edges
      ~IPA.gather_edges_3d
      ~IPA.get_buffer
      ~IPA.get_extra_state
      ~IPA.get_parameter
      ~IPA.get_submodule
      ~IPA.graph_state_dict
      ~IPA.half
      ~IPA.inverse_RT
      ~IPA.ipu
      ~IPA.load_graph_state_dict
      ~IPA.load_state_dict
      ~IPA.modules
      ~IPA.named_buffers
      ~IPA.named_children
      ~IPA.named_modules
      ~IPA.named_parameters
      ~IPA.parameters
      ~IPA.register_backward_hook
      ~IPA.register_buffer
      ~IPA.register_forward_hook
      ~IPA.register_forward_pre_hook
      ~IPA.register_full_backward_hook
      ~IPA.register_full_backward_pre_hook
      ~IPA.register_load_state_dict_post_hook
      ~IPA.register_module
      ~IPA.register_parameter
      ~IPA.register_state_dict_pre_hook
      ~IPA.requires_grad_
      ~IPA.reset_parameter
      ~IPA.set_extra_state
      ~IPA.share_memory
      ~IPA.state_dict
      ~IPA.to
      ~IPA.to_empty
      ~IPA.train
      ~IPA.type
      ~IPA.xpu
      ~IPA.zero_grad
   
   

   
   
   .. rubric:: Attributes

   .. autosummary::
   
      ~IPA.T_destination
      ~IPA.call_super_init
      ~IPA.device
      ~IPA.dump_patches
      ~IPA.training
   
   